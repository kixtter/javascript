let resultado;

//PI
resultado = Math.PI;

//Redondear
resultado = Math.round(2.8);
resultado = Math.round(2.2);
resultado = Math.round(2.6);
resultado = Math.round(2.5);
resultado = Math.round(2.4);

//Redondear hacia arriba de forma forzada
resultado = Math.ceil(2.1);

//Redondear hacia abajo de forma forzada
resultado = Math.floor(2.9);

//Raiz
resultado = Math.sqrt(144);

//Absoluto
resultado = Math.abs(-500);

//Potencia
resultado = Math.pow(8, 3);

//Mínimo
resultado = Math.min(3, 2, 1, 12, -3);

//Máximo
resultado = Math.max(3, 2, 1, 12, -3);

//Aleatorio
resultado = Math.random() * 20;

//Aleatorio dentro de un rango
resultado = Math.floor(Math.random() * 30);
console.log(resultado);