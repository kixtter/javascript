let i = 0;//Inicializar el while

while (i < 100)//Condición
{
    if (i % 2 === 0)
    {
        console.log(`Número par: ${i}`);
    }
    else
    {
        console.log(`Número impar: ${i}`);
    }

    i++;//Incremento
}